#!/bin/bash

# Replaces committed golden fixtures with responses from the running API.
# The resulting fixture diff must be reviewed before it is committed.

docker exec digital-growth-charts-server-growth-api-1 \
    python -m tests.regression.accept_goldens --accept
